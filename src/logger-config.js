class LoggerConfig {
    constructor(level, appenders, filter, config) {
        this.level = level;
        this.appenders = appenders;
        this.filter = filter;
        this.config = config;
    }
}